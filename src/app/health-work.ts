export class HealthWork {
    constructor (date?: Date, healthIndex?: number, workIndex?: number) {
        this.date = date;
        this.healthIndex = healthIndex;
        this.workIndex = workIndex;
    }

    

    healthIndex : number;
    workIndex : number;
    date : Date;

}
